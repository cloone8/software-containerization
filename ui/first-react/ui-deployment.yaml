apiVersion: apps/v1
kind: Deployment
metadata:
  name: first-react-deployment
  labels:
    app: first-react-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: first-react-app
  template:
    metadata:
      labels:
        app: first-react-app
    spec:
      containers:
      - name: first-react-app
        image: localhost:32000/ui:v3
        imagePullPolicy: Always
        ports:
        - containerPort: 80

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-ingress
spec:
  tls:
  - hosts:
      - www.quotebook.io
    secretName: webapp-tls
  rules:
  - host: www.quotebook.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: quote-react-app
            port:
              number: 80
